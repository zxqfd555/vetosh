"""Indexer package.

Note: importing the Pathway graph (``vetosh.indexer.graph``) pulls in the heavy
``pathway`` dependency. The cache module is intentionally dependency-free so it
can be imported and tested in isolation.
"""
